/**
  * Drag and drop functionality for checkers
  */
  
var GhostChecker = function(color) {
	var img;
	if ( color == 'red' ) {
		img = $('img').attr('src', 'graphics/red-piece.png');
		console.log('red');
	}
	if ( color == 'black' ) {
	
	}
	
	
	
	
	
	return img;
};